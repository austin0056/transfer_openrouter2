# 推荐：只配置管理入口，其余在浏览器打开 /admin/ 填写并保存到 data/config.json
# ADMIN_KEY 不要写入 CONFIG_FILE
ADMIN_KEY=

# 可选：运行时 JSON 路径（默认 data/config.json）
# CONFIG_FILE=data/config.json

# --- 以下为可选：本地调试或不用管理面板时再填写 ---
# OPENROUTER_API_KEY=
# GATEWAY_API_KEY=
# HTTPS_PROXY=
# DATABASE_URL=
# 托管 PostgreSQL 无 pgvector 时：执行 migrations/001_init_no_pgvector.sql，并设 EMBEDDING_USE_PGVECTOR=false
# EMBEDDING_USE_PGVECTOR=false
